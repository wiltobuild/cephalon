# Arsenal and deterministic optimizer

User authorization: September 6, 2026, “implement,” following the audited product and implementation plan for wiltobuild/cephalon.

Implement the usable builder and search foundation: authoritative validation, explicit simulation outcomes, item artwork, responsive navigation, a recognizable mod layout, bounded deterministic optimization, ownership constraints, local saves/shareable snapshots, and same-scenario comparison. Preserve engine formulas and explain their current limitations.

Source eligibility corrections are necessary: the audit's live optimizer selected Chilling Reload and Magnetic Strafe for Braton because the existing catalog tagged shotgun mods as general. Resolve compatibility using a reviewed, pinned WFCD metadata snapshot, independently of damage statistics. Retain the engine's weapon-specific eligibility checks.

Keep future work visible: current-game enemy-model replacement, proven exact optimization, full frame/loadout/shard modeling, richer acquisition planning, accounts and supported integrations. These must not be represented as completed by a catalog UI.
