# Builder readability and arcane update — 2026-09-07

- Slot polarity now has a separate, prominent labelled control. Rank controls have sufficient width for R5/R10. Mod effect text is visible on compact cards; Primed cards use a distinct ivory/gold frame.
- Replaced arcane stack inputs with a conditional damage switch and an icon-based arcane picker. The selected arcane's declared stack cap is used when enabling bonuses or choosing an arcane with bonuses enabled. Validation now accepts caps above 20 (Primary Blight uses 40); the engine still clamps each arcane to its own cap.
- Base DPS is independently calculated with zero kill/arcane stacks; the main DPS and burst values follow the active bonus state. Equipped arcane passive effects retain existing engine semantics. Special arcane handler and uptime coverage remain unchanged; this is not an overhaul of those formulas.
- Removed technical caveat prose and search implementation commentary from the builder. API/source coverage information remains intact. Damage details are now an always-visible styled panel below configuration, above the collection. Stats use percentages, seconds, shots/s and multiplier symbols.
- Validation: 72 service tests and 8 web tests passed. All 12 existing browser flows passed, plus a new rank/card/detail readability test. Arcane flow checks damage and reload changes, persistence through optimization, and conditional/base toggle behavior. Extended service regression confirms Primary Blight's 40-stack cap validates. Production build passed. Desktop populated build, arcane icon picker, and phone rendering inspected.
