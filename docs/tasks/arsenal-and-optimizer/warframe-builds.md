# Warframe sample builds and temporary customization — 2026-09-07

Implemented all 35 builds from `docs/guides/build-guide-samples.md`, retrieved from GitHub branch `build-guide-samples` at commit `3b61d7cde524e9f8477a76f3bffc4305631042dd`. The earlier 193-entry compilation is not used for the build directory. The sample's names, guide prose and listed arsenal stats are retained. Each guide has frame artwork, abilities/passive, numbered stats, an accessible zero-origin radar chart with a distinct 100% baseline, mod cards, arcanes, and shard guidance. Jade retains both aura slots. Arsenal Warframe inspection links to filtered guides; the dedicated directory is in navigation.

Follow-up: added compact cards, larger frame names, mission tags and playstyle filters. A compact five-slot shard tray sits below performance. Explicit recommendations populate slots; unresolved alternatives remain colorless. The full recommendation text remains available. Optional alternatives are not all equipped simultaneously. Hydroid's prose associates a corrosive ability effect with Crimson, which is not a selectable effect for that color; those three slots remain flexible rather than introducing an invalid pairing.

Customize enables temporary regular/aura/Exilus mod and rank editing through a compatible mod collection. A server endpoint validates frame compatibility, ranks, duplicate variants, slot types, five shard choices, and each color/tier/effect combination, then calls the existing Warframe engine through BuildService. The shard toggle includes/excludes shard effects; Reset restores guide values. Drafts are not saved. Live stats can differ from the author's listed arsenal snapshot; no adjustment is invented to force the engine to match it. Conditional arcane/Helminth combat simulations and capacity/forma budgeting are not introduced in this pass. Weapon-specific shard effects do not modify the four ability-stat chart axes.

Catalog corrections: source metadata determines universal/frame-specific eligibility; engine Exilus helper is exported without changing formulas. The missing Primed Sure Footed utility entry is represented with no ability/pool stat modifiers; knockdown resistance is not calculated in this panel. Metadata rank text supplies readable card effects.

Validation: 72 service tests and 11 web unit tests passed, including every approved sample with shards on/off and a +15 Strength Tauforged Crimson regression. All 18 browser tests passed across the weapon and Warframe flows, plus the final phone navigation check. Production build and boundary checks passed. Desktop directory, frame editor, shard tray and phone screenshots inspected. No commit, push or deployment performed.
